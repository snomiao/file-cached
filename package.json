{
  "name": "file-cached",
  "version": "0.0.16",
  "description": "Simply cache your object in ./cache.json file.",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "type": "module",
  "exports": {
    "import": "./dist/index.js",
    "types": "./index.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build index.ts --outdir dist",
    "prepack": "bun test && rm -rf dist && bun run build",
    "release": "bunx standard-version && git push --follow-tags origin main && npm publish",
    "test": "bun test",
    "prerelease": "bun run build && bun run test"
  },
  "dependencies": {
    "just-curry-it": "^5.3.0",
    "obj-cached": "latest"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@types/bun": "^1.1.3",
    "@types/jest": "^29.5.12",
    "rambda": "^9.2.0",
    "semantic-release": "^23.0.8",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "homepage": "https://github.com/snomiao/file-cached",
  "types": "./index.ts",
  "module": "index.ts",
  "main": "index.js"
}
